<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Diretório de cache para arquivos temporários -->
    <cache-path
        name="reports_cache"
        path="reports/" />

    <!-- Diretório externo de downloads (se necessário) -->
    <external-path
        name="downloads"
        path="Download/" />

    <!-- Diretório externo de documentos -->
    <external-files-path
        name="external_files"
        path="." />
</paths>